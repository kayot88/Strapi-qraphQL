import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Checkout extends Component {
  render() {
    return <div>Checkoute</div>;
  }
}

// Checkout.propTypes = {};

export default Checkout;
